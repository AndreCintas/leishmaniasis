Start
    Import necessary libraries
    Open output file to log information

    Get system information and current time
    Print information to the output file

    Function read_data(xlsx, sheet):
        Read Excel data
        Convert month column to datetime
        Set month column as index
        Fill missing values
        Create lag columns (lag1, lag2, lag3)
        Return DataFrame

    Read data for the city "fortaleza"
    Remove unnecessary columns
    Split data into train and test
    Normalize data

    Function ann1(optimizer, activation, neurons):
        Create ANN model
        Compile model
        Return ANN model

    Function lstm1(optimizer, activation, neurons):
        Create LSTM model
        Compile model
        Return LSTM model

    Define parameters for GridSearchCV for ANN and SVM

    For each combination of columns:
        Create and train the ANN model using GridSearchCV
        Evaluate the ANN model and log metrics

    For each combination of columns:
        Create and train the LSTM model using GridSearchCV
        Evaluate the LSTM model and log metrics

    For each combination of columns:
        Create and train the SVM model using GridSearchCV
        Evaluate the SVM model and log metrics

    Find the best models based on RMSE, MSE, MAE

    Print the results of the best models for ANN, LSTM, and SVM to the output file

    Close the output file
End
